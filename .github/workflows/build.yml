name: Build Custom Frida

on:
  workflow_dispatch:
    inputs:
      frida_version:
        description: 'Frida version (e.g. 17.5.2)'
        required: true
        default: '17.5.2' # 17.5.2 버전으로 기본값 고정
      custom_name:
        description: 'Custom name (replaces "frida" everywhere)'
        required: true
        default: 'ajeossida'
      arch:
        description: 'Target architecture(s)'
        required: true
        default: 'android-arm64,android-x86_64' # 요청하신 두 아키텍처 기본 지정
        type: choice
        options:
          - android-arm64
          - android-x86_64
          - android-arm64,android-x86_64
          - android-arm64,android-arm,android-x86_64,android-x86
      port:
        description: 'Custom port (empty = keep 27042)'
        required: false
        default: ''
      extended:
        description: 'Extended anti-detection (D-Bus, symbols, binary sweep)'
        required: false
        type: boolean
        default: true
      temp_fixes:
        description: 'Stability fixes (perfetto skip, cloak detach)'
        required: false
        type: boolean
        default: false

jobs:
  build:
    runs-on: ubuntu-22.04
    timeout-minutes: 120

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: System info
        run: |
          echo "CPUs: $(nproc)"
          echo "RAM: $(free -h | grep Mem | awk '{print $2}')"
          echo "Disk: $(df -h / | tail -1 | awk '{print $4}') free"
          python3 --version
          git --version

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential curl git python3 unzip

      # 1. 17.5.2용 NDK r25c 캐시 설정
      - name: Cache NDK
        uses: actions/cache@v4
        id: ndk-cache
        with:
          path: build/android-ndk-r25c
          key: ndk-r25c-linux

      # 2. 17.5.2용 NDK r25c 다운로드 주소 세팅
      - name: Download NDK
        if: steps.ndk-cache.outputs.cache-hit != 'true'
        run: |
          mkdir -p build
          curl -L -o build/android-ndk-r25c-linux.zip \
            https://dl.google.com/android/repository/android-ndk-r25c-linux.zip
          cd build && unzip -q android-ndk-r25c-linux.zip && rm android-ndk-r25c-linux.zip

      # Cache Frida source tree
      - name: Cache Frida source
        uses: actions/cache@v4
        id: frida-cache
        with:
          path: build/frida
          key: frida-${{ inputs.frida_version }}-source

      - name: Clone Frida
        if: steps.frida-cache.outputs.cache-hit != 'true'
        run: |
          git clone --recurse-submodules --branch ${{ inputs.frida_version }} --depth 1 \
            https://github.com/frida/frida.git build/frida

      # 아키텍처 순회 빌드 로직 (Wipe 자동 리셋 기능 포함)
      - name: Run Build for each architecture
        run: |
          IFS=',' read -r -a arch_array <<< "${{ inputs.arch }}"
          
          for arch in "${arch_array[@]}"; do
            echo "============================================================"
            echo "[START] Building for architecture: $arch"
            echo "============================================================"
            
            cd build/frida
            git checkout -- .
            git submodule foreach --recursive 'git checkout -- . || true'
            git clean -fdx || true
            rm -rf build/
            cd ../..
            
            # 3. 파라미터 ndk-path를 r25c 경로로 정확하게 매칭
            CMD="python3 build.py --version ${{ inputs.frida_version }}"
            CMD="$CMD --name ${{ inputs.custom_name }}"
            CMD="$CMD --arch $arch"
            CMD="$CMD --skip-clone --ndk-path build/android-ndk-r25c"
            CMD="$CMD --verify"

            if [ -n "${{ inputs.port }}" ]; then
              CMD="$CMD --port ${{ inputs.port }}"
            fi

            if [ "${{ inputs.extended }}" = "true" ]; then
              CMD="$CMD --extended"
            fi

            if [ "${{ inputs.temp_fixes }}" = "true" ]; then
              CMD="$CMD --temp-fixes"
            fi
            
            echo "Executing: $CMD"
            $CMD
            
            echo "[SUCCESS] Finished building for $arch"
          done

      - name: List artifacts
        run: |
          echo "=== Build artifacts ==="
          ls -lh output/
          echo ""
          echo "=== Binary verification ==="
          for f in output/*; do
            if [[ ! "$f" == *.gz ]]; then
              count=$(strings "$f" | grep -c "frida" || true)
              echo "$f: $count residual 'frida' strings"
            fi
          done

      - name: Upload server artifacts
        uses: actions/upload-artifact@v4
        with:
          name: ${{ inputs.custom_name }}-server-${{ inputs.frida_version }}
          path: output/${{ inputs.custom_name }}-server-*.gz
          retention-days: 30
          if-no-files-found: warn

      - name: Upload gadget artifacts
        uses: actions/upload-artifact@v4
        with:
          name: ${{ inputs.custom_name }}-gadget-${{ inputs.frida_version }}
          path: output/${{ actions.github.event.inputs.custom_name }}-gadget-*.gz
          retention-days: 30
          if-no-files-found: ignore

      - name: Upload uncompressed binaries
        uses: actions/upload-artifact@v4
        with:
          name: ${{ inputs.custom_name }}-${{ inputs.frida_version }}-uncompressed
          path: |
            output/*
            !output/*.gz
          retention-days: 7
